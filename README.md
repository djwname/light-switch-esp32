# light-switch-esp32
```
git clone --no-checkout https://github.com/djwname/light-switch-esp32.git
cd light-switch-esp32
git sparse-checkout init --cone
git sparse-checkout set server  # only clone server
git checkout main
